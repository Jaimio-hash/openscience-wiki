---
title: "ヘッドレスサービスおよびブラウザアクセス"
last_update:
  date: '2026-09-14'
---

# ヘッドレスサービスおよびブラウザアクセス {/* #headless-service-and-browser-access */}

ローカルサービスはヘッドレスバックエンドとローカルホストブラウザインターフェイスを提供します。 SSH ComputeホストとRemote.Itブラウザのペアリングとは違います。 クライアントを接続する前に、ホストの認証および認証ストレージ設定を選択します。

## ターゲット選定と発見 {/* #target-selection-and-discovery */}

| セレクター | スコープ |
| --- | --- |
| `--port PORT` | ローカルホストサービスポートをオーバーライド。 有効な整数 1–65535 |
| `--app-path PATH` | インストールしたアプリケーション実行可能を選択します。 実行可能なパスを使用して、任意のプロジェクトフォルダではなく、 |
| `--config-root PATH` | 開発ビルドの設定オーバーライド; パッケージ起動が拒否される |
| `OPEN_SCIENCE_CONFIG_ROOT` / `OPEN_SCIENCE_STORAGE_ROOT` | Explicit の構成の発見は支えられるオーバーライドを過します |
| 自動検出 | 生産の前に開発構成を試みて下さい、死んだ/不健康な候補者をスキッピングして下さい |

明示的な設定場所は、そのディレクトリへの発見を制限します。 開始または停止する前に、意図したプロファイルのステータスを確認してください。 ステータスが `running:false` を返す場合は、以下のライフサイクルコマンドを実行します。 開いたデスクトップのウィンドウは別のサービス/プロファイルを使用するかもしれません。

サービス状態ファイルは `web-service.json` です。 認証の失敗は、記録されたプロセスを殺す権限ではありません。 シャットダウンコードは、診断のためのライブ不健康なレコードを保存し、別のプロセスによって再利用されている可能性があるPIDのシグナル伝達を回避します。

## 初期化と確認の信頼性 {/* #readiness */}

`open-science init` を使用して、アプリを起動せずにデフォルト設定ディレクトリを用意します。 `--profile` エイリアス `--config-root` 開発プロファイルのオーバーライドがサポートされるだけ; パッケージビルド制限を迂回しません。 Debianパッケージは、アプリケーションと一緒にCLIをインストールします。 Codexの準備とログインについては、[ターミナル セットアップ](cli.md#terminal-setup)を参照してください。

`start --no-open`を意図した後、`open-science doctor --json`を実行します。 `ready`全体を見極め、個別にチェックして次のアクションを提案した。 プロセスの終了コードは、単純さの予測ではありません。 既存の認証されたローカルインターフェースでサービスを維持します。 このヘッドレスサービスは、Remote.Itを構成したり、パブリックエンドポイントを公開したりしません。

## ライフサイクルコマンド {/* #lifecycle-commands */}

| コマンド | 結果 | オプションと境界 |
| --- | --- | --- |
| `open-science start` | バックエンドを起動し、ブラウザを開く | デフォルトポート 44100 |
| `open-science start --no-open` | ブラウザを開くことなく起動します。 | 意図的なローカルサービスセッションに使用する |
| `open-science status --json` | 印字機対応サービスの状態 | 欠落したサービスリターン `{"running":false}` 1 を終了します。 |
| `open-science url` | 認証されたブラウザー URL を印刷する | ローカルアクセス権限を保持します。 公開された例に貼り付けない |
| `open-science stop` | 認証された優雅なシャットダウンを要求 | スタンル状態ファイルからPIDを盲目に信号しない |
| `open-science stop --json` | 操業停止の結果を報告する | 下記の結果表をご覧ください。 |

これらのコマンドのうち、**ステータスとサポート停止 `--json`; 起動とURLは起動しません。** です。 ローカル`start --json`チェックは`invalid_cli_usage`を返し、何も開始する前にコード2を終了します。 スクリプト起動のために、`start --no-open` を実行し、`status --json` を実行します。

### シュートダウン結果 {/* #shutdown-outcomes */}

| JSON `result` | 意味する |
| --- | --- |
| `already-stopped` | ライブサービス記録が見つかりません |
| `daemon-stopped` | 認証されたスタンドアローンデーモンが終了 |
| `web-service-stopped` | ウェブサービス停止のお知らせ デスクトップアプリケーションが稼働しているまま |

拒否されたリクエストまたは欠落したシャットダウン期限は失敗を返します。 エラーを読んで、実際のターゲットの状態を調べます。 コマンドが返されたため、サービスが停止するように報告しないでください。

## 認証とブラウザアクセス {/* #authentication-and-browser-access */}

SDKはローカルサービスを発見し、ローカル認証トークンを読み取り、リクエストヘッダで送信します。 通常のヒューマン/JSON/JSONL タスク出力は、そのトークンを印刷しません。 `url`は、認証されたブラウザエントリを生成する意図的な例外です。

ローカルホストサービスは、別のコンピュータから自動的にアクセスできません。 リモートブラウザアクセスは、独自の構成されたアクセスモード、ペアリングおよび信頼できるブラウザのライフサイクルを使用します。 SSH Compute は、ジョブを構成したリモート実行ホストに送信します。

Remote.ItのペアリングとSSHジョブの[遠隔計算](../guides/remote-compute.md)に[リモートブラウザーアクセス](../guides/remote-access.md)を使用します。 ローカルサービスのドキュメンテーション URL を変更することで、各フローが構成されます。

### ヘッドレス Linux のクレデンシャルストレージ {/* #credential-storage-on-headless-linux */}

デフォルトはOS保護された貯蔵です。 Linuxヘッドレスバックエンドで、使用可能なキーリングなしで、明示的な代替手段を選択します。

<p className="example-label"><strong>例</strong> Linuxヘッドレスサービスをファイル認証ストレージで起動</p>

~~~bash
open-science start --credential-store=file --no-open
~~~

| おすすめ商品 | ビーキャビター |
| --- | --- |
| 省略されたオプション / --credential-store=os | OS保護された店を要求して下さい |
| --credential-store=ファイル | 暗号化されていない設定管理の秘密をLinuxヘッドレスでのみ許可する |
| デスクトップ、macOS または Windows 起動 | ファイルモードはサポートされていません |
| すでに実行されているバックエンド | 明示的なモード選択は拒否されます。 そのプロセスのモードを変更しない |
| スタートアップ | モードを再度指定して下さい; 保存された好みではありません |

ファイルモードは、設定ルートでsettings.jsonとCertificate.jsonを使用して、アトミック書き込みとPOSIXモード0600を使用します。 file:v1: 値が base64 でエンコードされている **暗号化されていない** です。 読むことができる人は、秘密を回復することができます。 これらのファイルはリポジトリ、画像、サポートレポートから除外します。

選択は、新しい/更新された設定管理プロバイダのキー、アプリ管理サブスクリプショントークン、GitHub/literatureキーおよび共有MCP/OAuthの秘密に適用されます。 パスワード/保護された計算データは別のOS貯蔵の条件を維持します; 外部のエージェント・フレームワークのログインストアは、独自のルールに従います。 このオプションでサンドボックスが無効になっています。

暗号化された値が自動的に移行されず、元のOSボルトを必要としません。 利用できない場合は、通常のサポートフォームからクレデンシャルを再入力してください。 ファイルの refs は、古いリリースと互換性のない explicit ファイルモードが必要です。 OS ストレージにクレデンシャルを返すには、OS モードで再起動し、vault が利用可能なときに明示的に置換します。

構成が容器の取り替えを生き残らなければならないとき永続的な貯蔵を使用して下さい。 Shutdownは構成を保持します; 使い捨てコンテナのファイルシステムを置き換えると、削除できます。 [クレデンシャルストレージ契約](https://github.com/aipoch/open-science/blob/v0.27.0/packages/open-science/CLI.md) を参照してください。

## アプリケーション更新の動作 {/* #application-update-behavior */}

`open-science update`はインストールされたアプリケーションを更新します。 npmクライアントを別々に更新します。 コマンドは、必要なときにローカルサービスを開始し、その後に利用可能なサービスを残すことができます。

| `update --json` 結果発表 | 通訳・通訳 |
| --- | --- |
| `up-to-date` | 新しいバージョンが見つかりません |
| `install-started` | 更新者は取付けの handoff を受け入れました; 最終インストールバージョンは、その呼び出しによって検証されていません |
| `manual-action-required` | 報告されたインストーラパス/次のステップに従ってください |
| `blocked` | アクティブリサーチは、現場のアップデートを防止します。 検査検査 `blockedBy` |

サポートはサービス機能`update-cli-v1`を要求します。 古いインストールは、推測されたリモート手順の代わりに手動更新を必要とすることができます。 印刷された結果を保持し、インストール後にアプリケーションバージョンを確認します。

[ライフサイクルと発見の実装](https://github.com/aipoch/open-science/blob/v0.26.0/packages/open-science/cli.mjs), [構成の発見](https://github.com/aipoch/open-science/blob/v0.26.0/packages/open-science/config-root.mjs). タスクフラグと終了コードについては、[CLI](./cli.md) を参照してください。 プログラマティックコールについては、[タスクSDK](./api.md) を参照してください。
